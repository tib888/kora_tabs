Converts `music xml` format to a printable `svg` or `ascii` tab for kora instrument.
Also able to transcribe `wav` files into tabs in `music xml`, `svg` or `ascii` format.
The tuning.toml can be expanded to take care of various kora tunings and string numbers.
Usage: start from command line and provide the necessary command line parameters.
